# Ansible
ansible-core==2.14.0
ansible-lint==5.3.2 # pin version since latest is incompatible with our ansible version

jsonpatch
PyYAML
kubernetes
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
